<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.novelsite.mapper.ChapterMapper">

    <resultMap id="ChapterResultMap" type="Chapter">
        <id column="id" property="id"/>
        <result column="novel_id" property="novelId"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="word_count" property="wordCount"/>
        <result column="chapter_order" property="chapterOrder"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="novel_name" property="novelName"/>
    </resultMap>

    <select id="findById" resultMap="ChapterResultMap">
        SELECT c.*, n.name as novel_name
        FROM chapter c
        LEFT JOIN novel n ON c.novel_id = n.id
        WHERE c.id = #{id}
    </select>

    <select id="findByNovelId" resultMap="ChapterResultMap">
        SELECT * FROM chapter WHERE novel_id = #{novelId} ORDER BY chapter_order
    </select>

    <select id="findByNovelIdAndOrder" resultMap="ChapterResultMap">
        SELECT * FROM chapter WHERE novel_id = #{novelId} AND chapter_order = #{chapterOrder}
    </select>

    <select id="findPrevChapter" resultMap="ChapterResultMap">
        SELECT * FROM chapter
        WHERE novel_id = #{novelId} AND chapter_order &lt; #{currentOrder}
        ORDER BY chapter_order DESC
        LIMIT 1
    </select>

    <select id="findNextChapter" resultMap="ChapterResultMap">
        SELECT * FROM chapter
        WHERE novel_id = #{novelId} AND chapter_order &gt; #{currentOrder}
        ORDER BY chapter_order ASC
        LIMIT 1
    </select>

    <select id="getMaxOrder" resultType="int">
        SELECT COALESCE(MAX(chapter_order), 0) FROM chapter WHERE novel_id = #{novelId}
    </select>

    <insert id="insert">
        <selectKey keyProperty="id" resultType="long" order="AFTER">
            SELECT last_insert_rowid()
        </selectKey>
        INSERT INTO chapter (novel_id, title, content, word_count, chapter_order, create_time, update_time)
        VALUES (#{novelId}, #{title}, #{content}, #{wordCount}, #{chapterOrder}, datetime('now'), datetime('now'))
    </insert>

    <update id="update">
        UPDATE chapter SET
            title = #{title},
            content = #{content},
            word_count = #{wordCount},
            update_time = datetime('now')
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM chapter WHERE id = #{id}
    </delete>

    <delete id="deleteByNovelId">
        DELETE FROM chapter WHERE novel_id = #{novelId}
    </delete>

</mapper>
